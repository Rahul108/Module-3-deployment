apiVersion: v1
kind: Service
metadata:
  name: node-app-canary-service
  labels:
    app: node-app
spec:
  selector:
    app: node-app
  ports:
  - port: 80
    targetPort: 5000
    protocol: TCP
  type: ClusterIP
